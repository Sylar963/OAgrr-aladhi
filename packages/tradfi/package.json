{
  "name": "@oggregator/tradfi",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file-if-exists=../../.env src/index.ts",
    "build": "tsc",
    "start": "node --env-file-if-exists=../../.env dist/index.js",
    "lint": "biome lint .",
    "typecheck": "pnpm --filter @oggregator/protocol build && pnpm --filter @oggregator/core build && tsc --noEmit",
    "test": "vitest",
    "test:run": "pnpm --filter @oggregator/protocol build && pnpm --filter @oggregator/core build && vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.0",
    "@fastify/websocket": "^11.0.0",
    "@oggregator/core": "workspace:*",
    "@oggregator/protocol": "workspace:^",
    "fastify": "^5.12.5",
    "pino": "^9.0.0",
    "ws": "^8.21.3",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/ws": "^8.18.1",
    "pino-pretty": "^13.1.3",
    "tsx": "4.23.15",
    "typescript": "^5.5.0",
    "vitest": "4.1.11"
  }
}
